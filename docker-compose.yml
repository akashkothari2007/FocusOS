services:
  db:
    image: postgres:16
    container_name: focusos_db
    environment:
      POSTGRES_USER: focusos
      POSTGRES_PASSWORD: focusos
      POSTGRES_DB: focusos
    ports:
      - "5432:5432"
    volumes:
      - focusos_pg:/var/lib/postgresql/data

volumes:
  focusos_pg: